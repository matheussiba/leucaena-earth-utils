# R scripts

**Language:** English headers and comments.

| File | Summary |
|:-----|:--------|
| `baixar_estados_br.R` | Download Brazil state polygons (IBGE 2020) with `geobr` and save to GeoPackage. Set `output_path` before running. |

Run from RStudio or `Rscript r/scripts/baixar_estados_br.R` after `install.packages(c("geobr", "sf"))`.
